// first rust program to display hello world
// it is the main function that executes when program starts
fn main(){
   println!("Hello world");
}

// to run this program will run command rustc main.rs  same like in node (node index.js) 
// rustc main.rs command will generate the main file which contains our output to run that type ./main in terminal
// we can do the same thing using cargo package manager